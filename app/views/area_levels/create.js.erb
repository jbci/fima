<% if @area_level.errors.empty? %>
  // add the row to the table, show alert and hide modal
  $('#area_levels_table tbody').append(' <%= j render "row", area_level: @area_level %>');
  $('.alert-success').html("El producto ha sido creado con éxito").show();
  $('#area_level-modal').modal('hide');
<% else %>
  // show first error
  alert('<%= @area_level.errors.full_messages[0] %>');
<% end %>
