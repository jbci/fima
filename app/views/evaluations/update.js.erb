<% if @evaluation.errors.empty? %>
  // add the row to the table, show alert and hide modal
  $('table tbody tr#evaluation_<%= @evaluation.id %>').replaceWith(' <%= j render "row", evaluation: @evaluation %>');
  $('.alert-success').html("El evaluationo ha sido modificado con éxito").show();
  $('#evaluation-modal').modal('hide');
<% else %>
  // show first error
  alert('<%= @evaluation.errors.full_messages[0] %>');
<% end %>
