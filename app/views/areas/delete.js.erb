<% if @area.errors.empty? %>
  // add the row to the table, show alert and hide modal
  $('table tbody tr#area_<%= @area.id %>').remove();
  $('.alert-success').html("El area ha sido eliminado con éxito").show();
  $('#area-modal').modal('hide');
<% else %>
  // show first error
  alert('<%= @area.errors.full_messages[0] %>');
<% end %>
