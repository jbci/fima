<div>
		<a href="" class="close cd-img-replace">Close</a>
		<h2><%= @project.title %></h4>
				<hr></hr>
				<% if i.mark == true %>
	        <p class="text-right">Días para recibir observaciones <%= number_to_spans i.remaining_days %></p>
	      <% else %>
	        <p class="text-right">Días para enviar observaciones <%= number_to_spans i.remaining_days %></p>
	      <% end %>
		<h4><%= @project.description %></h4>

		<div>
		<% unless @project.image.size.nil? %>
			<%= image_tag @project.image.url %>
		<% end %>
		</div>

</div>
