<div>
		<a href="" class="close cd-img-replace">Close</a>
		<h2><%= @project.title %></h4>
				<hr></hr>
				<p class="text-right">Días para recibir observaciones <span class="counter"><%= @project.remaining_days %></span></p>
		<h4><%= @project.description %></h4>

		<div>
		<% unless @project.image.size.nil? %>
			<%= image_tag @project.image.url %>
		<% end %>
		</div>

</div>
