<% if @project.errors.empty? %>
  // add the row to the table, show alert and hide modal
  $('table tbody tr#project_<%= @project.id %>').remove();
  $('.alert-success').html("El project ha sido eliminado con éxito").show();
  $('#project-modal').modal('hide');
<% else %>
  // show first error
  alert('<%= @project.errors.full_messages[0] %>');
<% end %>
